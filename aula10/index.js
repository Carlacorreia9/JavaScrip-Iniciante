/**
 * Artméticos
 * + Adição / Concatenação
 * - Subtração
 *   Multiplicação *
 * / Divisão
 * % Resto da divisão
 * ** Potenciação
 * ++ Incremento
 * -- Decremento
 *  - /*
 */

const numl = 5; // number
const num2 = 10; // number
const num3 = 15; // number
console.log((num1 + num2) * num3); // Adição

// Aritmeticos é uma operação matemática que envolve números e operadores matemáticos. Os operadores aritméticos mais comuns são:
// Adição (+), Subtração (-), Multiplicação (*), Divisão (/), Módulo (%), Potenciação (**), Incremento (++) e Decremento (--).
// A adição é a operação de somar dois ou mais números. Por exemplo, 5 + 10 = 15.
// A subtração é a operação de subtrair um número de outro. Por exemplo, 10 - 5 = 5.
// A multiplicação é a operação de multiplicar dois ou mais números. Por exemplo, 5 * 10 = 50.
// A divisão é a operação de dividir um número por outro. Por exemplo, 10 / 5 = 2.

/**
    ()* Operadores Aritméticos
    * ** Potenciação
    * % Resto da divisão
    * 
 */
const num4 = 5; // number
const num5 = 10; // number
const num6 = 15; // number
console.log(num4 * num5 / num6); // Adição



let contador = 1; // number
contador++; // Incremento
console.log(contador); // Exibe o valor da variável contador no console do navegador

// Decremento é a operação de subtrair 1 de um número. Por exemplo, 5 - 1 = 4. O decremento é representado pelo operador -- em JavaScript. Por exemplo, contador--;
// O incremento é a operação de adicionar 1 a um número. Por exemplo, 5 + 1 = 6. O incremento é representado pelo operador ++ em JavaScript. Por exemplo, contador++;
// O operador de incremento pode ser usado antes ou depois da variável. Por exemplo, ++contador ou contador++. A diferença é que o primeiro incrementa a variável antes de usá-la, enquanto o segundo incrementa a variável depois de usá-la. Por exemplo:

let contador2 = 1; // number
contador2++; 
contador2++; // Incremento
contador2++; // Incremento
contador2++; // Incremento
console.log(contador); // Exibe o valor da variável contador no console do navegador
// O operador de incremento pode ser usado antes ou depois da variável. Por exemplo, ++contador ou contador++. A diferença é que o primeiro incrementa a variável antes de usá-la, enquanto o segundo incrementa a variável depois de usá-la. Por exemplo:

let contador3 = 1; // number
++contador3;
++contador3;
++contador3;
++contador3; // Incremento
console.log(contador2, contador3); // Exibe o valor da variável contador no console do navegador

// O operador de decremento pode ser usado antes ou depois da variável. Por exemplo, --contador ou contador--. A diferença é que o primeiro decrementa a variável antes de usá-la, enquanto o segundo decrementa a variável depois de usá-la. Por exemplo:
let contador4 = 1; // number
contador4--;
contador4--;
contador4--;
contador4--; // Decremento
console.log(contador4); // Exibe o valor da variável contador no console do navegador

// O operador de decremento pode ser usado antes ou depois da variável. Por exemplo, --contador ou contador--. A diferença é que o primeiro decrementa a variável antes de usá-la, enquanto o segundo decrementa a variável depois de usá-la. Por exemplo:
let contador5 = 1; // number
contador5--;
contador5--;
contador5--;
contador5--; // Decremento
console.log(contador5); // Exibe o valor da variável contador no console do navegador

/*
Incremento = ++ é um operador que adiciona 1 ao valor da variável. Por exemplo, se a variável x tem o valor 5, então ++x resulta em 6.
Decremento = -- é um operador que subtrai 1 do valor da variável. Por exemplo, se a variável x tem o valor 5, então --x resulta em 4.
 O operador de incremento pode ser usado antes ou depois da variável. Por exemplo, ++x ou x++. A diferença é que o primeiro incrementa a variável antes de usá-la, enquanto o segundo incrementa a variável depois de usá-la. 
 */

 const passo = 2;
 let contador = 0; // number

 contador = contador + passo; // Adição
 console

 const passo = 50 ; // number
 let contador = 0; // number
 contador += passo; // Adição contador = contador + passo; // Adição


 let contador = 0; // number
 contador += 2;
    contador += 2; // Adição
    contador += 2; // Adição
    console.log(contador); // Exibe o valor da variável contador no console do navegador

    // NaN - Not a Number
    // NaN é um valor especial que representa um resultado inválido ou indefinido de uma operação matemática. Por exemplo, se você tentar dividir um número por zero, o resultado será NaN.

    const num1 = 10; // number
    const num2 = '10'; // string
    const num3 = 5; // number
    const resultado = num1 + num2; // string
   




