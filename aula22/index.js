/**
 Opradores Lógicos
 O operador Logico é um operador que realiza operações lógicas em valores booleanos. Os operadores lógicos mais comuns são:
  - && (E) - Se ambos os valores forem verdadeiros, o resultado será verdadeiro.
    - || (OU) - Se pelo menos um dos valores for verdadeiro, o resultado será verdadeiro.
    - ! (NÃO) - Inverte o valor lógico. Se o valor for verdadeiro, o resultado será falso e vice-versa.
    
    && -> AND (E) - Se ambos os valores forem verdadeiros, o resultado será verdadeiro.
    || -> OR (OU) - Se pelo menos um dos valores for verdadeiro, o resultado será verdadeiro.
    ! -> NOT (NÃO) - Inverte o valor lógico. Se o valor for verdadeiro, o resultado será falso e vice-versa.
    - Se o valor for verdadeiro, o resultado será falso e vice-versa.
 */
 //console.log(true && true && false && true); // false
 //console.log(true && true && true && true); // true
   // console.log(true && false && true && true); // false
   // console.log(true && true && false && true); // false
   // console.log(true && true && true && false); // false

  // const expressaoAnd = true && true && false && true; // false
  //  console.log(expressaoAnd); // false



//
  const usuario = 'Carla'; // nome do usuário
    const senha = '123456'; // senha do usuário

    const vaiLogar = usuario === 'Carla' && senha === '123456'; // comparação entre usuário e senha
    console.log(vaiLogar); // true porque o usuário e a senha estão corretos

    

    


