// Não podemos criar constantes com palavras reservadas do JavaScript, como let, var, const, if, else, etc.
// Não podemos criar constantes com nomes que já foram utilizados anteriormente, como nome, nome2, etc.
//Constantes precisam ter nomes significativos e descritivos, como nomeCompleto, idade, altura, peso, etc.
//Constantes não podem começar com números, como 1nome, 2idade, 3altura, etc.
//Constantes não podem conter espaços, como nome completo, idade atual, altura em metros, etc.
//Não pode começar o nome de uma constante com letra minúscula, como nomeCliente, idade, altura, peso, etc.
//Constantes não podem conter caracteres especiais, como @, #, $, %, &, *, etc.
//Não podem conter espaços ou traços, como nome completo, idade atual, altura em metros, etc.
// Utilizamos cameLcase para nomear variáveis e constantes, como nomeCompleto, idadeAtual, alturaEmMetros, etc.
// Utilizamos o padrão PascalCase para nomear classes, como NomeCompleto, IdadeAtual, AlturaEmMetros, etc.
// Utilizamos o padrão snake_case para nomear arquivos, como nome_completo.js, idade_atual.js, altura_em_metros.js, etc.
//Case-sensitive: JavaScript é case-sensitive, ou seja, diferencia letras maiúsculas de minúsculas. Por exemplo, nome e Nome são variáveis diferentes. Portanto, devemos ter cuidado ao nomear variáveis e constantes para evitar confusões e erros no código. Além disso, devemos seguir as convenções de nomenclatura para facilitar a leitura e compreensão do código por outros desenvolvedores. Por exemplo, podemos usar o padrão camelCase para variáveis e constantes, como nomeCompleto, idadeAtual, alturaEmMetros, etc. E o padrão PascalCase para classes, como NomeCompleto, IdadeAtual, AlturaEmMetros, etc.
// Não pode modificar o valor de uma constante, como nomeCliente, idade, altura, peso, etc.
// Não utilizer VAR, utilizar LET e CONST, como nomeCliente, idade, altura, peso, etc.

let nome = 'Junior'; // string
// + - */ / % ** ++ --

const primeiroNumero = 5; // number
const segundoNumero = 10; // number
const conta = primeiroNumero * segundoNumero; // number
const conta2 = primeiroNumero / segundoNumero; // number
const conta3 = primeiroNumero + segundoNumero; // number
const conta4 = primeiroNumero - segundoNumero; // number
const conta5 = primeiroNumero % segundoNumero; // number
const conta6 = primeiroNumero ** segundoNumero; // number
